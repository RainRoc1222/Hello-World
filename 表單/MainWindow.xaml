<Window x:Class="表單.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:表單"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="400">

    <Border  Padding="10">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Margin="0 0 10 0">Apply</Button>
                <Button Grid.Column="1">Reset</Button>
                <Button Margin="10 0 0 0" Grid.Column="2">Refesh</Button>
            </Grid>
            <TextBlock FontWeight="Bold">Pulse Properties</TextBlock>
            <TextBlock>Descriptioln</TextBlock>
        </StackPanel>
    </Border>

</Window>
